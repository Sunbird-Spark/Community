# API endpoint changes

The API endpoints for content and user operations are unchanged. The merged services (knowlg-service, lern-service) expose the same API surface as the individual services in ED. No changes are required in client applications that call these APIs.

